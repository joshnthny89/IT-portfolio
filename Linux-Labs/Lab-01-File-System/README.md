# Lab 1 - File System Navigation

## Linux Fundamentals Lab

This repository contains Linux/macOS labs I have completed while studying cybersecurity and IT concepts.

### Objective

Learn basic file system navigation using the macOS terminal.

### Commands Used

- pwd
- ls
- ls -la
- cd
- mkdir
- rmdir

### Steps Performed

1. Used `pwd` to identify the current working directory.
2. Used `ls` to list the contents of the home directory.
3. Used `ls -la` to display all files and directories, including hidden files and detailed permissions.
4. Used `cd Library` to navigate into the Library directory.
5. Used `mkdir CyberLabs` to create a new directory named CyberLabs.
6. Verified the directory was created by listing directory contents.
7. Used `rmdir CyberLabs` to remove the empty directory.
8. Verified the directory was successfully removed.
9. Navigated between directories using the `cd` command to better understand file system structure.

### Results

Successfully navigated the macOS file system, viewed directory contents and permissions, created and removed directories, and practiced using common terminal commands for file system management.

### Screenshots

<img width="577" height="457" alt="image" src="https://github.com/user-attachments/assets/cc501c94-0052-4e5e-b39b-1d8c1ed176a5" />

<sub><em>*In this screenshot I utilize `pwd` to identify the current working directory, I then use `ls` to list the contents of the home directory, and finally using `ls -la` to display all files and directories, including hidden files and detailed permissions.*<sub><em></em></sub>

<img width="775" height="461" alt="image" src="https://github.com/user-attachments/assets/4f71199a-b241-461c-8a05-92eee06f9a5b" />
<img width="737" height="154" alt="image" src="https://github.com/user-attachments/assets/6f447e6c-b44e-496b-badd-f6f0b511ada5" />

<sub><em>*In the 2 screenshots above I used `cd Library` to navigate into the Library directory, and then `ls` to list the contents of the Library directory.*<sub><em></em></sub>

<img width="774" height="464" alt="image" src="https://github.com/user-attachments/assets/ec626297-666a-4a77-8500-6e8f5b9d3971" />
<img width="740" height="157" alt="image" src="https://github.com/user-attachments/assets/d5f2ca00-32fc-4d2e-8b36-fedfc91fd738" />

<sub><em>*In the 2 screenshots above I created within  the Library directory a directory called "CyberLabs" using `mkdir CyberLabs`, after creating the directory I used `ls` within the Library directory to verify that the "CyberLabs" directory was created.*<sub><em></em></sub>

<img width="757" height="458" alt="image" src="https://github.com/user-attachments/assets/86fcb797-8c1c-435f-a2d6-a01c47038fbb" />
<img width="737" height="187" alt="image" src="https://github.com/user-attachments/assets/01644990-7fc7-49e1-a8a2-f23330d0a348" />

<sub><em>*In the 2 screenshots above I navigate to the Library directory with `cd Library`, and once in the the Library directory i use `rmdir CyberLabs` to remove the previously created "CyberLabs" directory from the Library directory. The running the `ls` command to display the contents of the Library directory verifying the "CyberLabs" directory was removed.*<sub><em></em></sub>


<img width="546" height="235" alt="image" src="https://github.com/user-attachments/assets/a29987a7-a5b0-4dd0-a84f-3aae2631426d" />

<sub><em>*I used `mkdir Evidence` and `mkdir Reports` to create directories within "CyberLabs" directory for future labs.*<sub><em></em></sub>
### Skills Demonstrated

- Command-Line Navigation
- File System Exploration
- Directory Management
- Linux/macOS Terminal Usage
